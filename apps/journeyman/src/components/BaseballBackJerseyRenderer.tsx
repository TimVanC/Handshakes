/** Moved to the shared @handshakes/jerseys package (monorepo extraction).
 *  This shim keeps every existing import path working. */
export * from "@handshakes/jerseys/BaseballBackJerseyRenderer";
export { default } from "@handshakes/jerseys/BaseballBackJerseyRenderer";
